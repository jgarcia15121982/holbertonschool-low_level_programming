/**
 * add - adds two integers and returns the result
 * @a: First integer to add.
 * @b: Second integer to add
 *
 * Return: Returns the adding result between a and b
 */
int add(int a, int b)
{
	return (a + b);
}
