/**
 * mul - Multiplies two integers and returns the result
 * @a: First integer to add.
 * @b: Second integer to add
 *
 * Return: Returns the product result between a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
